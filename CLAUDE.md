# CLAUDE.md

@AGENTS.md
@firmware/AGENTS.md

## Claude Code

This file is intentionally a thin Claude Code adapter. Shared cross-agent rules live in `AGENTS.md`; firmware-specific rules live in `firmware/AGENTS.md`.

For new firmware `.h` and `.c` files, use the templates in @firmware/FILE_TEMPLATES.md.
